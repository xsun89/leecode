#include <iostream>
#include <cstring>
#include <cstdlib>

using namespace std;
char status[8][8];

int main (int ac, char *av[])
{
    memset(status, -1, sizeof(status));
    string line;

    while(getline(cin, line))
    {


    }
    return 0;
}